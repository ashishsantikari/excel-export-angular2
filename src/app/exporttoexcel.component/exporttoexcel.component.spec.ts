import { ExporttoexcelDirective } from './exporttoexcel.directive';

describe('ExporttoexcelDirective', () => {
  it('should create an instance', () => {
    const directive = new ExporttoexcelDirective();
    expect(directive).toBeTruthy();
  });
});
